(function() {
  'use strict';

  Controller.$inject = [];

  function Controller() {
    angular.extend(this, {

    });
  }

  angular
    .module('ngang.controllers', [])
    .controller('Controller', Controller);

})();